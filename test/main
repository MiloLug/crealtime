//usr/local/bin/crealtime "$0" { "$@" }; exit

#include <stdio.h>


int main(int argc, char *argv[]) {
    printf("argc: %i\n\n", argc);
    for(int i = 1; i < argc; i++) {
        printf("arg%i: %s\n", i, argv[i]);
    }
    return 0;
}
